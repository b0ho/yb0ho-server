type Image {
  id: Int!
  title: String!
  location: String!
  tags: [String!]
  round: Int!
  round_win: Int!
  game: Int!
  game_win: Int!
}

type Query {
  image(title: String!): Image
}

type Mutation {
  addImage(title:String!): Boolean!
}